all:
  vars:
    apt_cache_server: 192.168.1.211
  children:
    master: # kubernetes master
      hosts:
    workers: # kubernetes nodes
      hosts:
        192.168.1.222: # test-machine
        192.168.1.223: # test-machine2
    cacheservers: # apt-cache-ng mirroring
      hosts:
        192.168.1.211:
      vars:
        apt_cache_server: 127.0.0.1
    docker:
      children:
        master:
        workers:
